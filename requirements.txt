# DDNet 依赖（版本来自 conda env `lerobot` 的 `pip list --format=freeze`，请勿用系统默认 Python 查版本）
#
# 安装前请先: conda activate lerobot
#
# GPU（CUDA 12.8，与当前环境中的 torch 构建一致）：
#   pip install -r requirements.txt
#
# 若仅需 CPU 版 PyTorch，请删掉下面两行 --extra-index-url 与 +cu128 后缀，并改从 pytorch.org 选 CPU 轮子说明安装。

--extra-index-url https://download.pytorch.org/whl/cu128

torch==2.10.0+cu128
torchvision==0.25.0+cu128

numpy==2.2.6
scipy==1.17.1
librosa==0.11.0
torchnet==0.0.4
visdom==0.2.4
thop==0.1.1.post2209072238
torchsummary==1.5.1
tqdm==4.67.3
matplotlib==3.10.9
scikit-learn==1.8.0
seaborn==0.13.2
six==1.17.0
setuptools>=80.0.0
